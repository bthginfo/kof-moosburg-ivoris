
  # Untitled Make test (Copy)

  This is a code bundle for Untitled Make test (Copy). The original project is available at https://www.figma.com/design/mYIJwe7DCEAai7pM78stJU/Untitled-Make-test--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  